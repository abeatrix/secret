<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Whisperers</title>
</head>
<body>
    <ul>
        <% whisperers.forEach((user, i) => { %>
            <li>
                <a href='/whisperer/<%= whisperers[i]._id %>'> <%= whisperers[i].name %> </a>
                <form action ='/whisperer/<%=whisperers[i]._id %>?_method=DELETE' method='POST'><input type=submit value='DELETE'></form>
            </li>
        <% }) %>
    </ul>
</body>
</html>
